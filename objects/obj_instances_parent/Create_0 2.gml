/// obj_ySortedParent – Create Event

// Extra offset you can tweak in children if needed
ysort_offset = 0;

// If you want some children NOT to be sorted, you can toggle this:
ysort_enabled = true;
