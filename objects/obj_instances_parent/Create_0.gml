// obj_instances_parent - Create

// Turn on y-sorting by default for all children
ysort_enabled = true;

// Offset if you need to tweak where the “feet” are
ysort_offset = 0;

// Initial depth (will be updated every Step anyway)
depth = -bbox_bottom;
