// frist objective for player

var first_objective = "Chase after Misty to find the exit of the maze to the library";

// line space between each line
var line_spacing = 35;

// max width to wrap text
var max_width = 650;

// actually drawing the text
draw_text_ext(200, 290, first_objective, line_spacing, max_width);

draw_text(350, 400, "(Press enter to continue)");